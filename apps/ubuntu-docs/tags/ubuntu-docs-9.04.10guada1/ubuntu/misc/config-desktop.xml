<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN" 
	"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
<!ENTITY % globalent SYSTEM "../../libs/global.ent">
%globalent;
<!ENTITY % cdo-C SYSTEM "../../libs/cdo-C.ent">
%cdo-C;
<!ENTITY % gnome-menus-C SYSTEM "../../ubuntu/libs/gnome-menus-C.ent">
%gnome-menus-C;
<!ENTITY % xinclude SYSTEM "../../libs/xinclude.mod">
%xinclude;
<!ENTITY language "en">
<!ENTITY ubuntu '<phrase>Ubuntu</phrase>'>
]>
		<article id="desktop-tips" status="complete">
		<articleinfo>
			<title>Advanced Desktop Configuration</title>
    	&legalnotice;
        &copyright;
        &disclaimer;
        &publisher;
	</articleinfo>
			  <para>This section details some tips for using and customising Ubuntu&apos;s desktop environment (called <application>GNOME</application>), and the file manager (called <application>Nautilus</application>).</para>

		<sect1 status="complete">
				<title>Log in automatically when the computer starts</title>
				<para>To log in automatically when the computer starts:</para>
				<procedure>
					<step>
						<para>
							Choose &login-screen-setup;.
						</para>
					</step>
					<step>
						<para>Select <guibutton>Security</guibutton>, and turn on <guilabel>Enable Automatic Login</guilabel>.
						</para>
					</step>
					<step>
						<para>Choose the account you want to log in as.
						</para>
					</step>
				</procedure>
			<para>This is not recommended for most computers, as it is not secure and may give other people access to your information.</para>
		</sect1>	

		<sect1 status="complete">
			<title>Open files with administrative privileges from the file manager</title>
			<para>To open files with administrative privileges from the file manager, you need to add a script to <application>Nautilus</application>.
			</para>
			<warning><para>Opening files with administrative privileges is a security risk. Always ensure that you are familiar with the file that you are opening.</para></warning>
			<procedure>
				<step><para>Open a new file to edit in the relevant directory:
						
							<screen>gedit $HOME/.gnome2/nautilus-scripts/Open\ as\ Administrator</screen>
					</para>
				</step>
				<step>
						<para>Enter these lines in the new file:</para>
						<programlisting>for uri in $NAUTILUS_SCRIPT_SELECTED_URIS; do
gksudo "gnome-open $uri" &amp;
done</programlisting>
				</step>
				<step>
						<para>Save the edited file (<ulink url="../sample/nautilus_open_as_administrator">an example</ulink>)
						</para>
				</step>
				<step>
						<para>Make the edited file executable:
							<screen>chmod +x $HOME/.gnome2/nautilus-scripts/Open\ as\ Administrator</screen>
						</para>
				</step>
			</procedure>
						<para>You can now open a file as an administrator by right-clicking on it and choosing <menuchoice>
								<guimenuitem>Scripts</guimenuitem>
								<guimenuitem>Open as Administrator</guimenuitem>
							</menuchoice>.
						</para>
		</sect1>
     <sect1 id="fonts" status="complete">
		<title>Install Extra Fonts</title>
				<para>This section describes how to install extra fonts from the Ubuntu archives.</para>
							<para>For <emphasis role="bold">international
              fonts</emphasis>, install these packages from the
              <quote>Universe</quote> repository (see <ulink type="help"
              url="ghelp:add-applications">Add Applications</ulink>):
							</para>
			<itemizedlist>
				<listitem><para>xfonts-intl-arabic</para></listitem>
				<listitem><para>xfonts-intl-asian</para></listitem>
				<listitem><para>xfonts-intl-chinese</para></listitem>
				<listitem><para>xfonts-intl-chinese-big</para></listitem>
				<listitem><para>xfonts-intl-european</para></listitem>
				<listitem><para>xfonts-intl-japanese</para></listitem>
				<listitem><para>xfonts-intl-japanese-big</para></listitem>
				<listitem><para>xfonts-intl-phonetic</para></listitem>
			</itemizedlist>
							<para>For <emphasis role="bold">Microsoft TrueType core
              fonts</emphasis>, install the
              <application>msttcorefonts</application> package from the
              <quote>Multiverse</quote> repository (see <ulink
              type="help" url="add-applications">Add
              Applications</ulink>).
							</para>
							<para>For <emphasis role="bold">Ghostscript
              fonts</emphasis>, install the
              <application>gsfonts-x11</application> package (see <ulink
              type="help" url="ghelp:add-applications">Add
              Applications</ulink>).
							</para>
							<para>After installing any new font, rebuild the font information files by entering in a Terminal:
							</para>
							<screen>sudo fc-cache -f -v</screen>
			<para>If you prefer to download individual fonts by hand, you can install them simply and easily by opening a file manager, and typing <application>fonts:///</application> into the location bar (see <ulink type="help" url="ghelp:user-guide#nautilus-location-bar">the file manager manual</ulink> for how to use the location bar). Then you can simply drag the font you downloaded into the group of existing fonts.</para>
		</sect1>
		<sect1 status="complete">
				<title>RAR Archiver</title>
					<procedure>
						<step>
							<para>Install the rar package from the
              <quote>Multiverse</quote> repository (see <ulink
              type="help" url="ghelp:add-applications">Add
              Applications</ulink>).</para>
						</step>
						<step>
							<para>Run:
							<screen>
sudo ln -fs /usr/bin/rar /usr/bin/unrar</screen>
							</para>
						</step>
						<step>
							<para>To open a rar file, double click on it.
							</para>
						</step>
					</procedure>
		</sect1>
		<sect1 status="complete">
			<title>Advanced Desktop Applets</title>
				<para><application>gDesklets</application> is a system for bringing mini programs (desklets), such as weather forecasts, news tickers, system information displays, or music player controls, onto your desktop, in a symbiotic relationship of eye candy and usefulness.</para>
				<procedure>
					<step>
							<para>Install the <application>gdesklets</application> and
              <application>gdesklets-data</application> packages from
              the <quote>Universe</quote> repository (see <ulink
              type="help" url="ghelp:add-applications">Add
              Applications</ulink>).
							</para>
					</step>
					<step>
							<para>Choose: 
								&gdesklets;.
							</para>
					</step>
					<step><para>For more information, visit the <ulink url="http://www.gdesklets.org/">gDesklets website</ulink>.</para>
					</step>
				</procedure>
		</sect1>
		<sect1 status="complete">
			<title>Hide Recent Documents in the Places menu</title>
				<para>To hide <guilabel>Recent Documents</guilabel> from the <guilabel>Places</guilabel> menu, open a terminal and run the command:</para><screen>chmod 400 ~/.recently-used</screen>
				<para>To show the menu again, run the command:</para><screen>chmod 600 ~/.recently-used</screen>
		</sect1>
		<sect1 status="complete">
			<title>Show the Computer, Home, and Trash desktop icons in GNOME</title>
				<procedure>
					<step>
						<para>Open the <application>Configuration Editor</application>, by running the program <filename>gconf-editor</filename> (see <ulink type="help" url="ghelp:user-guide#tools-run-app">the run application manual</ulink> for help on how to run an application without using the menu).
						</para>
					</step>
					<step>
						<para>Choose
							<menuchoice>
								<guimenu>apps</guimenu>
								<guisubmenu>nautilus</guisubmenu>
								<guimenuitem>desktop</guimenuitem>
							</menuchoice>.
						</para>
					</step>
					<step>
						<para>Turn on <guilabel>computer_icon_visible</guilabel>, <guilabel>home_icon_visible</guilabel>, and <guilabel>trash_icon_visible</guilabel>. The changes take effect immediately.
						</para>
					</step>
				</procedure>
		</sect1>
		<sect1 id="restartgnomewithoutreboot" status="complete">
			<title>Restart GNOME without rebooting the computer</title>
			<procedure>
				<step>
						<para>Save and close all open applications.</para>
			          </step>
			          <step>
            				<para>Use the <keycombo>
						<keycap>Ctrl</keycap>
						<keycap>Alt</keycap>
						<keycap>Backspace</keycap>
						</keycombo>
							 shortcut keys to restart X and GNOME.
					</para>
           			</step>
           			<step>
            				<para>If <keycombo>
                <keycap>Ctrl</keycap>
                <keycap>Alt</keycap>
                <keycap>Backspace</keycap>
							</keycombo>
              is disabled, in a terminal type
						<screen>sudo /etc/init.d/gdm restart</screen>
            </para>
						<para>(See <xref linkend="disableCtrlAltBackspace"/> to disable <keycombo>
								<keycap>Ctrl</keycap>
								<keycap>Alt</keycap>
								<keycap>Backspace</keycap>
							</keycombo>.)
						</para>
				</step>
			</procedure>
		</sect1>
		<sect1 id="disableCtrlAltBackspace" status="complete">
			<title>Preventing the Ctrl-Alt-Backspace shortcut keys from restarting X</title>
				<procedure>
					<step>
						<para>Make a backup copy of your existing <filename>/etc/X11/xorg.conf</filename> file:
							<screen>sudo cp /etc/X11/xorg.conf /etc/X11/xorg.conf_backup</screen>
						</para>
					</step>
					<step>
					<para>Open <filename>/etc/X11/xorg.conf</filename> for editing:
						<screen>sudo gedit /etc/X11/xorg.conf</screen>
					</para>
					</step>
					<step>
						<para>Add the following lines to the end of the file:
							<programlisting>Section	"ServerFlags"
Option	"DontZap"	"yes"
EndSection</programlisting>
						</para>
					</step>
					<step>
						<para>Save the edited file (<ulink url="../sample/xorg.conf_disablectrlaltbackspacegnome">an example</ulink>).</para>
					</step>
					<step>
						<para>The change will be in effect the next time you log in to GNOME. If you want the change to take effect immediately, restart X manually. (See <xref linkend="restartgnomewithoutreboot"/>.)</para>
					</step>
				</procedure>
		</sect1>

	</article>
